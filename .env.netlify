# Configuração de Ambiente para Netlify
# Este arquivo deve ser usado como referência para configurar as variáveis no Netlify Dashboard

# ====================================
# VARIÁVEIS DE AMBIENTE OBRIGATÓRIAS
# ====================================

# Banco de Dados Neon (Opcional - sistema funciona sem)
DATABASE_URL=postgresql://username:password@host/database
# Exemplo: postgresql://maria_flor:senha123@ep-cool-cloud-12345.us-east-1.aws.neon.tech/mariaflor?sslmode=require

# JWT Secret (Obrigatório para segurança)
JWT_SECRET=maria-flor-netlify-secret-2024-super-seguro

# Ambiente de execução
NODE_ENV=production

# ====================================
# INSTRUÇÕES PARA CONFIGURAR NO NETLIFY
# ====================================

# 1. Acesse o Netlify Dashboard
# 2. Vá para o seu site > Site settings > Environment variables
# 3. Adicione cada variável acima clicando em "Add variable"

# ====================================
# CONFIGURAÇÕES OPCIONAIS
# ====================================

# Timezone (Opcional)
TZ=America/Sao_Paulo

# URL do site (Automático)
URL=https://barestaurente.netlify.app

# ====================================
# NEON DATABASE - CONFIGURAÇÃO
# ====================================

# Para configurar o Neon:
# 1. Crie uma conta em https://neon.tech
# 2. Crie um novo projeto
# 3. Copie a connection string
# 4. Cole na variável DATABASE_URL acima

# Schema SQL básico (execute no Neon Console):
# 
# CREATE TABLE users (
#     id SERIAL PRIMARY KEY,
#     username VARCHAR(50) UNIQUE NOT NULL,
#     password_hash VARCHAR(255) NOT NULL,
#     name VARCHAR(100) NOT NULL,
#     role VARCHAR(20) NOT NULL,
#     permissions JSON,
#     active BOOLEAN DEFAULT true,
#     created_at TIMESTAMP DEFAULT NOW()
# );
#
# CREATE TABLE sales (
#     id VARCHAR(20) PRIMARY KEY,
#     customer_name VARCHAR(100),
#     total_amount DECIMAL(10,2),
#     payment_method VARCHAR(20),
#     status VARCHAR(20),
#     created_at TIMESTAMP DEFAULT NOW()
# );

# ====================================
# TESTE DE FUNCIONAMENTO
# ====================================

# Após configurar, teste estas URLs:
# https://barestaurente.netlify.app/ - Site principal
# https://barestaurente.netlify.app/api/status - Status da API
# https://barestaurente.netlify.app/api/auth/login - Endpoint de login